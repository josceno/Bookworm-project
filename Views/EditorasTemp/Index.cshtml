@model IEnumerable<Bookworm.Models.Editora>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nomefantasia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Endereco)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nomefantasia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Endereco)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdEditora">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdEditora">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEditora">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
